
<h1>Listing Rails</h1>

<table>
  <thead>
    <tr>
      <th>Generate</th>
      <th>Model</th>
      <th>User</th>
      <th>Name</th>
      <th>Email</th>
      <th>Encrypted password</th>
      <th>Salt</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rails.each do |rail| %>
      <tr>
        <td><%= rail.generate %></td>
        <td><%= rail.model %></td>
        <td><%= rail.User %></td>
        <td><%= rail.name %></td>
        <td><%= rail.email %></td>
        <td><%= rail.encrypted_password %></td>
        <td><%= rail.salt %></td>
        <td><%= link_to 'Show', rail %></td>
        <td><%= link_to 'Edit', edit_rail_path(rail) %></td>
        <td><%= link_to 'Destroy', rail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rail', new_rail_path %>
